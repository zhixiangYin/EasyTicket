[project]
name = "easyticket"
version = "0.1.0"
description = "A step-by-step ticket search agent built with Python"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
easyticket = "app.cli.main:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
